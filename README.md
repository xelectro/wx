Clone the repo
cd to WX folder
run uv sync
run uv run uvicorn wx_station2:app --host 0.0.0.0 --port 8080
stop the app by Ctrl-C
run uv sync again
and relaunch the previous us run command
navigate to browser http://127.0.0.1:8080

